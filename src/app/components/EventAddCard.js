"use client";

import React, { useState, useEffect } from "react";
import axios from "axios";

export default function EventaddCard ({
    
})

        {/* <EventModalDetails
        isOpen={optionModalEvent}
        onClose={() => setOptionModalEvent(false)}
        title="Options"
      >
          <div className="flex flex-row md:flex-col gap-1 mt-2 md:mt-0 md:ml-6 w-32">
          <button
            onClick={() => handleEditClick(eventInfo)}
            className="bg-green-500 text-white text-xs py-1 px-2 rounded-md hover:bg-green-600"
          >
            Modifier
          </button>
          <button
            onClick={() => handleDeleteClick(eventInfo)}
            className="bg-red-500 text-white text-xs py-1 px-2 rounded-md hover:bg-red-600"
          >
            Supprimer
          </button>
          <button
            onClick={handleDetailsClick}
            className="bg-blue-500 text-white text-xs py-1 px-2 rounded-md hover:bg-blue-600"
          >
            Détails
          </button>
        </div>
      </EventModalDetails> */}


    //   const handleOpenOptions = (eventInfo) =>{
    //     setOptionModalEvent(true);
    //   }

    //   const [optionModalEvent, setOptionModalEvent] = useState(false)